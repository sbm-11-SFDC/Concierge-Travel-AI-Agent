# .env.example - copy to .env and set your real key there (do NOT commit .env)

# Gemini API key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Local service ports
COORD_PORT=8000
TRAVEL_PORT=8005
MCP_PORT=8001
